# 2026-02-25

## Key Events
- Endurecimento do `make eval-runtime` para validar estado canonico de workspace (`workspace-state.json`) e contrato de nightly extraction.
- Execucao dos cenarios Red/Green para ausencia e invalidez de estado com restauracao controlada no mesmo ciclo.

## Decisions Made
- Manter `workspaces/main/.openclaw/workspace-state.json` como caminho unico de estado operacional no baseline F1.
- Exigir no gate runtime os parametros canonicos de nightly extraction (`0 23 * * *`, `America/Sao_Paulo`) alinhados ao PRD.

## Facts Extracted
- O gate runtime reprova quando `workspace-state.json` esta ausente ou invalido.
- O gate runtime reprova quando ha divergencia de baseline de heartbeat ou ciclo noturno.
